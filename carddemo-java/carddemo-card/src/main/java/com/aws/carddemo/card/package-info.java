/**
 * CardDemo card module - card management service (list, view, update).
 * Maps from COBOL programs: COCRDLIC (card list), COCRDSLC (card detail),
 * COCRDUPC (card update). Uses CARDDAT and CARDAIX VSAM files.
 * To be populated in later phases.
 */
package com.aws.carddemo.card;
