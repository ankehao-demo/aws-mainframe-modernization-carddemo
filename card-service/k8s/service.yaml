apiVersion: v1
kind: Service
metadata:
  name: card-service
  labels:
    app: card-service
spec:
  type: ClusterIP
  selector:
    app: card-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
